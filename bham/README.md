# bonelessham-api

Turbo HAMLOG を HTTP API 化するブリッジ。詳細は `CLAUDE.md` を参照。

## セットアップ

### 前提条件
- Windows 10/11
- Turbo HAMLOG インストール済み
- AutoHotkey v1.1 インストール済み
- Python 3.8+ インストール済み

### 起動
setup.bat を実行。
→ http://<your-host>:8669 でAPIが利用可能。

server 側との接続設定については `server/CLAUDE.md` の外部連携セクションを参照。
